import { StyleSheet } from 'react-native';

const noteView = StyleSheet.create({
  previewContainer: {
    flex: 1,
    height: '100%',
    padding: 10,
    marginBottom: 10,
  }
});

export default noteView;